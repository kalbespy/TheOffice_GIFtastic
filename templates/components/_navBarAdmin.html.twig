<div class="h-100 d-flex flex-column flex-shrink-0 p-3 bg-light" style="width: 280px;">
	<div class="d-flex align-items-center mb-3 mb-md-0 me-md-auto">
		<span class="fs-4">Dashboard</span>
	</div>
	<hr>
	<ul class="nav nav-pills flex-column mb-auto">
		<li class="nav-item">
			<a href="{{ path('app_gif_index')}}" class="nav-link {% if app.request.pathinfo starts with '/gif' %} active {% else %} link-dark {% endif %}" aria-current=" page">
				GIFs
			</a>
		</li>
		<li>
			<a href="{{ path('app_user_index')}}" class="nav-link {% if app.request.pathinfo starts with '/user/' %} active {% else %} link-dark {% endif %}">
				Users
			</a>
		</li>
	</ul>
	<hr>
	<div>
		<a href="{{ path("app_logout") }}" class="d-flex align-items-center link-dark text-decoration-none justify-content-between" id="dropdownUser2">
			<strong>Déconnexion</strong>
		</a>
	</div>
</div>
